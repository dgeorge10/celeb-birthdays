{
  "name": "celebs",
  "version": "0.1.0",
  "dependencies": {
    "express": "^4.17.1",
    "node-html-parser": "^1.1.15",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1",
    "request": "^2.88.0"
  },
  "scripts": {
    "start": "node server.js",
	 "heroku-postbuild": "npm install && npm run build",
    "dev": "nodemon server.js",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "proxy": "https://celebrity-birthdays.herokuapp.com/",
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
